import styled from 'styled-components';

const Container = styled.div`
   color:white;
   `

const LoginPage = () => {
  return(
  <Container>
      <div>로그인 페이지</div>
  </Container>
); 
};

export default LoginPage;
